// Package constants defines application-wide constants
package constants

// Pagination constants define default values for paginated queries
const (
	DefaultPageSize        = 20
	DefaultArticlesPerPage = 6
	MaxItemsPerPage        = 100
)

// Context keys
const (
	UserIDKey = "userID"
)
